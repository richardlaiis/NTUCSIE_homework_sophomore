\documentclass[11pt]{article}
\usepackage{xcolor} % Added to define code background color
\usepackage{graphicx}
\usepackage[margin=2cm, a4paper]{geometry}
\usepackage{setspace}
\usepackage{pdfpages}
\usepackage{float}
\usepackage{amsmath}
\usepackage{fancyvrb}
\usepackage{amssymb}
\usepackage{minted}
\usepackage{enumitem}

% --- Minted Global Settings ---
\definecolor{codebg}{rgb}{0.96,0.96,0.96} % A soft, light gray
\setminted{
    bgcolor=codebg,      % Applies the light gray background
    linenos=true,        % Adds line numbers on the left
    breaklines=true,     % Automatically wraps long lines of code
    frame=lines,         % Adds a solid line above and below the code block
    framesep=2mm,        % Adds padding between the frame and the code
    fontsize=\small,     % Keeps the font size slightly smaller than document text
    baselinestretch=1.1  % Gives the code lines slightly more vertical breathing room
}

\title{Numerical Methods Homework 5}
\author{B13902022 LAI, YU-CHI}
\date{}

\begin{document}
\maketitle

\noindent\textbf{Octave is used in all implementations of this homework!}
\section*{Problem 1}
\subsection*{(a)}
By the equations in lecture materials and provided boudary conditions, we have the following linear systems:
\begin{equation*}
\begin{bmatrix}
2h_0 & h_0 & 0 & \cdots & 0 \\
h_0 & 2(h_0+h_1) & h_1 & \cdots & 0 \\
0 & h_1 & 2(h_1+h_2) & \cdots & 0 \\
\vdots & \vdots & \vdots & \ddots & \vdots \\
0 & 0 & \cdots & h_{n-1} & 2h_{n-1}
\end{bmatrix}
\begin{bmatrix}
c_0 \\
c_1 \\
c_2 \\
\vdots \\
c_n
\end{bmatrix}
=
\begin{bmatrix}
z_0 \\
z_1 \\
z_2 \\
\vdots \\
z_n
\end{bmatrix}
\end{equation*}

Where the elements of vector $z$ are defined as:
\begin{align*}
z_0 &= \frac{3}{h_0}(a_1 - a_0) - 3f'(x_0) \\
z_j &= \frac{3}{h_j}(a_{j+1} - a_j) - \frac{3}{h_{j-1}}(a_j - a_{j-1}) \quad \text{for } j = 1, \dots, n-1 \\
z_n &= 3f'(x_n) - \frac{3}{h_{n-1}}(a_n - a_{n-1})
\end{align*}

After obtaining $c_j$'s, we can easily obtain $a,b,c$ using: $a_j=f(x_j)$, $b_j = \frac{1}{h_j}(a_{j+1} - a_j) - \frac{h_j}{3}(2c_j + c_{j+1})$, $d_j = \frac{c_{j+1} - c_j}{3h_j}$
% Options are now handled globally in the preamble
\inputminted{octave}{my_spline.m} 

\subsection*{(b)}

I use the following script to test the correctness of \texttt{my\_spline}. (Run \texttt{octave test\_spline.m}, and the resulted image generated by the script can be seen below the code.)
\inputminted{octave}{test_spline.m}
\begin{figure}[H]
    \centering\includegraphics[width=0.8\linewidth]{spline_plot.png}
    \caption{The output (Maximum difference between custom and built-in: 2.220446e-16)}
\end{figure}
\section*{Problem 2}
\subsection*{(a)}
% (Your mathematical derivation goes here)
\begin{align*}
    E(a,b,c) = \sum_{i=1}^{m} (y_i - (ax_i^2 + bx_i + c))^2 \\
    \frac{\partial E}{\partial a} = 0 \Rightarrow a\sum_{i=1}^{m} x_i^4 + b\sum_{i=1}^{m} x_i^3 + c\sum_{i=1}^{m} x_i^2 = \sum_{i=1}^{m} x_i^2 y_i \\
    \frac{\partial E}{\partial b} = 0 \Rightarrow a\sum_{i=1}^{m} x_i^3 + b\sum_{i=1}^{m} x_i^2 + c\sum_{i=1}^{m} x_i = \sum_{i=1}^{m} x_i y_i \\
    \frac{\partial E}{\partial c} = 0 \Rightarrow a\sum_{i=1}^{m} x_i^2 + b\sum_{i=1}^{m} x_i + cm = \sum_{i=1}^{m} y_i
\end{align*}
Therefore, we can obtain matrix $A$ and vector $z$ such that $A\begin{bmatrix}
    a \\ b \\ c
\end{bmatrix}=z$ using the three equations above:
$$A = \begin{bmatrix}\sum_{i=1}^{m} x_i^4 & \sum_{i=1}^{m} x_i^3 & \sum_{i=1}^{m} x_i^2 \\ \sum_{i=1}^{m} x_i^3 & \sum_{i=1}^{m} x_i^2 & \sum_{i=1}^{m} x_i \\ \sum_{i=1}^{m} x_i^2 & \sum_{i=1}^{m} x_i & m\end{bmatrix}$$
$$z = \begin{bmatrix}\sum_{i=1}^{m} x_i^2 y_i \\ \sum_{i=1}^{m} x_i y_i \\ \sum_{i=1}^{m} y_i\end{bmatrix}$$

\subsection*{(b)}
\inputminted{octave}{quad_least_square.m}

\subsection*{(c)}
I use the following script to test the correctness of \texttt{quad\_least\_square}. 

(Just run \texttt{octave test\_quad\_least\_square.m})

My function performs very well on $f_1(x)=e^x$, but performs poorly on $f_2=\sin{2\pi x}$ (See the resulted image generated by the script below the code.)
\inputminted{octave}{test_quad_least_square.m}
\begin{figure}[H]
    \centering\includegraphics[width=\linewidth]{least_squares_comparison.png}
    \caption{The performance on exponential function (Nice) and sine wave (Poor)}
\end{figure}

\end{document}